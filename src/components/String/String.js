import React from 'react';
import './String.css';

const constri = (props) => (
        <div className="StringDiv">
            <p className="outstring">" {props.string}. "</p>
        </div>
);

export default constri;
